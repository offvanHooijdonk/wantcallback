<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.wantcallback"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="22"/>

    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
    <uses-permission android:name="android.permission.READ_CALL_LOG"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppBaseTheme">

        <activity
            android:name="com.wantcallback.ui.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="com.wantcallback.ui.SetReminderActivity"
            android:label="@string/edit_reminder_title">
        </activity>

        <activity android:name="com.wantcallback.ui.RemindActivity">
        </activity>

        <service
            android:name=".service.InitializerIntentService">
        </service>
    </application>

</manifest>